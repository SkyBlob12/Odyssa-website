{
  "name": "odyssa-blog-automation",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Génération automatisée des articles du blog Odyssa (destinations + conseils) via Notion, Groq et Unsplash.",
  "scripts": {
    "generate": "node scripts/generate-weekly.mjs",
    "generate:dry": "node scripts/generate-weekly.mjs --dry-run",
    "build:listings": "node scripts/generate-weekly.mjs --listings-only",
    "backfill:tip-images": "node scripts/backfill-tip-images.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "sharp": "^0.33.5"
  }
}
